diff --git a/CMakeLists.txt b/CMakeLists.txt
index 50a80629..cb87fdea 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -26,6 +26,7 @@ set(GGML_CUDA_PEER_MAX_BATCH_SIZE 128)
 set(GGML_CUDA_GRAPHS ON)
 set(GGML_CUDA_FA ON)
 set(GGML_CUDA_COMPRESSION_MODE default)
+set(GGML_OPENVINO ON)
 
 if((CMAKE_OSX_ARCHITECTURES AND NOT CMAKE_OSX_ARCHITECTURES MATCHES "arm64")
     OR (NOT CMAKE_OSX_ARCHITECTURES AND NOT CMAKE_SYSTEM_PROCESSOR MATCHES "arm|aarch64|ARM64|ARMv[0-9]+"))
@@ -71,6 +72,11 @@ install(TARGETS ggml-base ${CPU_VARIANTS}
     FRAMEWORK DESTINATION ${OLLAMA_INSTALL_DIR} COMPONENT CPU
 )
 
+install(TARGETS ggml-openvino
+    RUNTIME DESTINATION ${OLLAMA_INSTALL_DIR} COMPONENT OPENVINO
+    LIBRARY DESTINATION ${OLLAMA_INSTALL_DIR} COMPONENT OPENVINO
+)
+
 check_language(CUDA)
 if(CMAKE_CUDA_COMPILER)
     if(CMAKE_VERSION VERSION_GREATER_EQUAL "3.24" AND NOT CMAKE_CUDA_ARCHITECTURES)
diff --git a/llama/llama.go b/llama/llama.go
index 582d4128..3fcda32e 100644
--- a/llama/llama.go
+++ b/llama/llama.go
@@ -131,6 +131,10 @@ func NewContextParams(numCtx int, batchSize int, numSeqMax int, threads int, fla
 	} else {
 		params.flash_attn_type = C.LLAMA_FLASH_ATTN_TYPE_DISABLED
 	}
+
+	if os.Getenv("OLLAMA_FLASH_ATTENTION") == "1" {
+		params.flash_attn_type = C.LLAMA_FLASH_ATTN_TYPE_ENABLED
+	}
 	params.type_k = kvCacheTypeFromStr(strings.ToLower(kvCacheType))
 	params.type_v = kvCacheTypeFromStr(strings.ToLower(kvCacheType))
 
diff --git a/ml/backend/ggml/ggml/.rsync-filter b/ml/backend/ggml/ggml/.rsync-filter
index 449ec9e5..beea3cd9 100644
--- a/ml/backend/ggml/ggml/.rsync-filter
+++ b/ml/backend/ggml/ggml/.rsync-filter
@@ -7,7 +7,12 @@ hide /include/ggml-webgpu.h
 include /cmake/
 include /cmake/common.cmake
 include /include/
+include /include/ggml-openvino.h
 include /src/
+include /src/CMakeLists.txt
+include /src/models/**/CMakeLists.txt
+include /src/models/***
+include /common/***
 include /src/ggml-blas/
 include /src/ggml-cpu/
 include /src/ggml-cpu/amx/
@@ -20,6 +25,10 @@ include /src/ggml-cuda/vendors/
 include /src/ggml-cuda/template-instances/
 include /src/ggml-hip/
 include /src/ggml-metal/
+include /src/ggml-openvino/
+include /src/ggml-openvino/openvino/
+include /src/ggml-openvino/openvino/op/
+include /src/ggml-openvino/openvino/pass/
 include src/ggml-vulkan/
 include src/ggml-vulkan/vulkan-shaders
 include CMakeLists.txt
@@ -27,6 +36,8 @@ include *.[chm]
 include *.cpp
 include *.cu
 include *.cuh
+include *.hpp
+include *.h
 include *.metal
 include *.comp
 include *.glsl
